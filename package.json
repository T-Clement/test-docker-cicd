{
  "name": "learn-docker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker-stop": "docker stop test-container && docker rm test-container",
    "docker-run" : "docker run --name test-container -d -p 8080:80 test-php"
  },
  "author": "",
  "license": "ISC"
}
